# ── Dependencies + lockfiles per workspace ──
node_modules/

# ── Secrets ──
.env
.env.local
.env.*.local

# ── OS ──
.DS_Store
*.log

# ── Bot data: ảnh upload + WhatsApp session + cache ──
data/uploads/*
data/templates/*
data/wa-session/
data/*.db
data/*.db.*
data/*.db-journal
data/*.db-shm
data/*.db-wal
prisma/data/
data/qr.png
data/*.png
data/backups/
data/screenshots/
!data/uploads/.gitkeep
!data/templates/.gitkeep

# ── whatsapp-web.js Puppeteer ──
.wwebjs_cache/
.wwebjs_auth/

# ── Next.js build artifacts ──
crm/.next/
crm/out/
crm/next-env.d.ts

# ── IDE + tooling ──
.vscode/
.idea/
.claude/

# ── Project archives ──
_archive_python/
